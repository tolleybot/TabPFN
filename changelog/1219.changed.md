The in-memory cache of built models is now on by default, so repeat fits of the same checkpoint skip rebuilding it and, on GPU, re-transferring its weights. Set `TABPFN_MODEL_CACHE_SIZE=0` to restore the previous behaviour.
