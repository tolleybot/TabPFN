`matplotlib` is now a direct dependency instead of behind the optional `viz` extra (which has been removed), since `skrub` (a new direct dependency, used by upcoming work) already requires it transitively.
